# Domain Docs

Ground Ball is a single-context repository.

Before domain or architecture work, read the root `CONTEXT.md` and any relevant ADRs under `docs/adr/`. Use the canonical vocabulary from `CONTEXT.md` and surface conflicts with existing decisions explicitly.
