# Archived Architecture Plans

These documents record historical implementation handoffs and completed plan ledgers. The active architecture overview lives in [`../architecture.md`](../../architecture.md), and the canonical current context lives in [`../../CONTEXT.md`](../../../CONTEXT.md).

Archived plans should not drive new implementation decisions without first checking the current runtime code and active docs.
