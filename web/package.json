{
  "name": "ground-ball-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "test": "vitest run && node --test scripts/package-dist-test.mjs",
    "package:sync": "node scripts/package-dist.mjs sync",
    "package:check": "node scripts/package-dist.mjs check"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "jsdom": "^30.0.1",
    "svelte": "^5.56.10",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
