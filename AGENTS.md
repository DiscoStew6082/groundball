# AGENTS.md instructions for /Volumes/Envoy/projects/baseball-rag

Use the tdd skill for dev work.
Use subagents wherever possible and a code review subagent should be run after every task.
You are not finished coding until your changes have been committed and any unstaged changes are explained.
If you have pushed to GitHub make sure the CI comes back green.
Use the Codex in-app Browser to troubleshoot this project. Start the local UI if needed with `uv run baseball-rag-ui`, open `http://127.0.0.1:7861/`, make the Browser visible, run a quick default-query smoke test, and keep the dev server running.
